# Sprite Util

## Usage
```cs
// Update sprite region
// [!] Make sure that Sprite->Region is enabled and sizes are configured
SpriteUtil.UpdateRegion(sprite, 4)

// Resize sprite by exact pixel size
SpriteUtil.ResizePx(sprite, 240, 320);
```